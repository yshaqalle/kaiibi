import { Text, View } from 'react-native';

export default function InventoryScreen() {
  return <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}><Text>Inventory (Task 11)</Text></View>;
}
